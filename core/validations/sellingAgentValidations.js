let sellingAgentValidations = {
    validateCreateAgent: (agent)=>{
        return new Promise((resolve, reject) => {
            resolve();
        });
    }
};

module.exports = sellingAgentValidations;
